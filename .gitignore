# Terraform local state and cache
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan

# Sensitive variable files — commit terraform.tfvars if it contains no secrets
# terraform.tfvars
